name: Actions

on: push

jobs:
  build:
  runs-on: ubuntu-latest
  steps:
    - name: Git Actions
      uses: actions/checkout@v1
    - name: List Files After Checkout
      run: |
        pwd
        ls -a
    - name: Srinivasarao